apiVersion: v1
kind: Service
metadata:
  name: llm-service
  labels:
    app: llm-api
spec:
  type: {{ .Values.service.type }}

  selector:
    app: llm-api
  
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http